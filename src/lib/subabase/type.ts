export type Habit = {
  id: number;
  name: string;
  streak: number;
  bestStreak: number;
  lastChecked: boolean;
  level: number;
};

export type FlameDetails = {
  color: string;
  size: string;
};
